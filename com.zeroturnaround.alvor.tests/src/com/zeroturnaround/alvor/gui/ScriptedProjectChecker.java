package com.zeroturnaround.alvor.gui;

import org.eclipse.core.resources.IProject;

/*
 * Meant for checking "alvor-test-projects"
 * Is used by ScriptedProjectTest
 * 
 * Checks project according to its name prefix (clean, build, change, ...)
 * Stores test results in "AlvorTestResults" folder and compares with
 */
public class ScriptedProjectChecker {
	public static void checkProject(IProject project) {
		
	}
}
