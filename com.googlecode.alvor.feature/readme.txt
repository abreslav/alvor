* if you want to export to directory or to archive then, when installing (as from local update-site)
  you need to uncheck "Group items by category" because feature export doesn't allow creating
  categories (unlike proper update-site). 